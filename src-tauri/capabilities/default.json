{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 창의 최소 권한 — 폴더 고르기 대화상자만. fs·shell 은 프론트가 쓰지 않아 뺐다 (2026-08-31 보안 점검)",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
